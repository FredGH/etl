version: 2
models:
   - name: dim_date
     description: the date dimension
     columns:
      - name: date
        description: the surrogate key
        tests:
          - not_null
          - unique
